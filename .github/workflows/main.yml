name: MSBuild CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Descargar el código
      uses: actions/checkout@v4

    - name: Configurar MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Restaurar paquetes NuGet
      # Apuntamos directamente al archivo .csproj
      run: nuget restore ModernizacionEcuadorPractico/ModernizacionEcuadorPractico.csproj

    - name: Compilar el proyecto
      # Compilamos directamente el archivo .csproj
      run: msbuild ModernizacionEcuadorPractico/ModernizacionEcuadorPractico.csproj /p:Configuration=Release
